# super-app

Aplicações: Despesas Fixas, Finanças, Lista de Compras, Saúde Familiar. Front único (`index.html`) e APIs em `api/`. Módulos em `modulos/`. Banco: alterações de estrutura ficam em `sql.sql` (executar no SQL Editor do Supabase quando precisar).