{
  "name": "super-app",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "generate-icons": "node scripts/generate-pwa-icons.js",
    "update-sw": "node scripts/update-sw-version.js",
    "build": "npm run generate-icons && npm run update-sw"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
